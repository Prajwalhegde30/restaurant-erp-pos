{
  "name": "@repo/pubsub",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --clean",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/logger": "*",
    "@repo/types": "*",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "6.0.3"
  }
}
